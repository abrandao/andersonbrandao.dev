<h1>Listing Tags</h1>
 
<table>
  <tr>
    <th>Tag:</th>
    <th></th>
  </tr>
 
  <% @tags.each do |tag| %>
    <tr>
      <td><%= tag.name %></td>
      <td><%= link_to 'Show', tag_path(tag) %></td>
      <td><%= button_to 'Apagar', tag_path(tag), method: :delete, data: {
        confirm: 'Você tem certeza?' } %></td>
    </tr>
  <% end %>
</table>
<br>
<%= link_to 'Nova Tag', new_tag_path %>